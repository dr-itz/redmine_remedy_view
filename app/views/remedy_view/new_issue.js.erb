try {
  $('#remedy2issue').show().html('<%= escape_javascript(render :template => 'issues/new.html') %>');
} catch (err) {
}
var h2 = $('#remedy2issue > h2');
h2.text("<%= @project %> - " + h2.text());
hideOnLoad();
