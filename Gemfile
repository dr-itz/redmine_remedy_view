group :test do
#  gem 'annotate'
  if !dependencies.any? { |d| d.name == "simplecov" }
    gem "simplecov", :require => false
  end
  if !dependencies.any? { |d| d.name == "shoulda" }
    gem "shoulda"
  end
end
